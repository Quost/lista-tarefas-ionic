import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'a05-lista-tarefas-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
